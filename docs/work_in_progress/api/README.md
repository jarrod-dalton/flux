# API Drafts

This folder stores API prototypes that are not currently package-supported.

`fluxCore_sim_api_plumber_draft.R` is a sunset prototype moved out of
`fluxCore/inst/` to avoid implying a stable, shipped API contract.
